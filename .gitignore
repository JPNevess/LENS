__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# Streams. The synthetic ones are built from lens/streams.py by the scripts in
# data/, or on demand the first time a run asks for one; the real ones are
# downloaded. The definitions are committed, the bytes are not.
data/*.arff
data/*.csv
*.arff.partial

# Everything a run produces. The code that produces it is what is committed.
results/
figures/output/
*.npz

.DS_Store
